[project]
name = "mantel-kohonen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic~=2.13",
]

[dependency-groups]
dev = [
    "ruff~=0.16",
    "jupyterlab~=4.6",
    "matplotlib~=3.11",
    "pytest~=9.1",
]
